_format_version: '3.0'
services:
  - name: product-cores-service
    url: https://api.sampleapis.com
    routes:
      - name: product-cores-route
        paths:
          - /product/css/cores
        strip_path: true
        methods:
          - GET
    plugins:
      - name: request-transformer
        config:
          replace:
            uri: /csscolornames/colors
